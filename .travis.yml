language: node_js
node_js:
  - '0.8'
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-joomla-component' ]; then cd .. && eval "mv $currentfolder generator-joomla-component" && cd generator-joomla-component; fi

